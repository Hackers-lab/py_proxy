# Python source caches
__pycache__/

# PyInstaller intermediate build artifacts
build/
*.spec.bak

# Build outputs (produced by CI / local builds — never commit)
dist/
build.log

# Generated by NetSplitTunnel.spec at build time — not a source file
version_info.txt
